/**
 * This package contains data access objects (DAOs) for the Gift Certificate application.
 * <p>
 * DAOs are responsible for accessing the database and performing CRUD operations on the application's data.
 * </p>
 * This package includes interface that define the DAO methods, as well as implementations of those interfaces that interact with the database.
 * <p>
 * All DAO implementations in this package use the JDBC for interacting with thedatabase.
 * JDBC provides a standard way of mapping Java objects to database tables and performing
 * database operations using object-oriented syntax.
 * </p>
 */
package com.epam.esm.dao;